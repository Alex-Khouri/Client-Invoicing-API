--- Further Development Opportunities ---

* Implement more strict password requirements (e.g. mixed character types, minimum length)
* Implement timed expiry system for user session tokens
* Read user session tokens from cookies (rather than URL arguments)
* Implement thread-locking mechanisms in all controllers to prevent race conditions with data processing
* Implement precautionary data validation checks within model classes (i.e. checking logically associated data fields)
* Implement unique ID management systems for all system entities (i.e. not just invoices), to avoid problems that can arise from uniquely identifying entities by text names (e.g. duplicate entity names caused by text character restrictions in some system layers, e.g. forward slashes in business names)
* More comprehensive use of type hints (e.g. function return types)

--- Considerations for Productionisation ---

* Ensure that end-to-end encryption is implemented (for ensuring password and session token security during authentication)
* Ensure that user passwords are encrypted before being stored in persistent storage
* When integrating the API with the front-end application, it's helpful for the date values of invoice entries to reflect the timezone of the user (rather than that of the API). Therefore the front-end application should provide one of the following:
	- Current date
	- Timezone (which is subsequently used by the API to generate the date)

--- Follow-Up Questions for Client ---

* Do invoices need to be re-approved after adjustments are made?
* Can invoices be edited after being sent to clients (e.g. if they're disputed), or is it necessary to create new invoices (and render existing ones 'Cancelled')?
* If invoices can be edited after being sent to clients, do they need re-approval?
* What kind of information should be included in billing history reports?
* Can invoices be deleted? If so:
	- Can staff delete them without manager approval, or should deletion privileges be reserved for managers?
	- Should complete removal of invoices be possible, or should they only be marked as 'Deleted' but retained within the system records?